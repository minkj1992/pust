[package]
name = "rust_json"
version = "0.1.0"
edition = "2021"
categories = ["serde", "serde_json", "custom_desrializer"]

[dependencies]
serde = {version = "1.0.160", features=["derive"]}
serde_json = "1.0.96"
